rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 開発環境用の一時的なルール（本番環境では認証を追加してください）
    match /orders/{document=**} {
      allow read, write: if true;
    }
    
    // 将来的にはこのようなルールを使用してください：
    // match /orders/{document=**} {
    //   allow read, write: if request.auth != null;
    // }
  }
}